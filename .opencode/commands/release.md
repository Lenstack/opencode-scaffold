---
description: Prepare release — version bump, changelog, git tag
---

<Add instructions here. This becomes the prompt when /release is run.>

You can:
- Reference files with @path/to/file
- Invoke subagents with @agent-name
- Load skills with @skill skill-name
- Run bash commands inline
